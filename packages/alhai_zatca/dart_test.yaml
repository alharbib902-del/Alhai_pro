# Exclude sandbox/integration tests from the default `flutter test` run.
# To include them: flutter test --tags integration
tags:
  sandbox:
    skip: "Requires ZATCA sandbox network access"
  integration:
    skip: "Requires external API access"
