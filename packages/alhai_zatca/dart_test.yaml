# Exclude sandbox/integration tests from the default `flutter test` run.
# To include them: flutter test --tags sandbox
tags:
  sandbox:
    # Metadata only. Tests with this tag need ZATCA sandbox network access.
  integration:
    # Tests that depend on external APIs or sandbox credentials.
