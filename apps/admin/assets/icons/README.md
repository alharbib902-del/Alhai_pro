# App Icon Placeholder

Place `app_icon.png` (1024 x 1024, PNG) here as an alternative icon source
location, or use `assets/branding/app_icon.png` which is the path configured
in `pubspec.yaml` for `flutter_launcher_icons`.

Both paths work -- the branding directory is the canonical location.
