# Fix 13 — CI/CD + فصل البيئات (🟡 مهم)
# الوقت: 6-8 ساعات | الأولوية: 13

أنت DevOps engineer خبير في GitHub Actions و Flutter.

## المهام
1. أنشئ `.github/workflows/ci.yml` (analyze + test + build-web)
2. أنشئ `.github/workflows/release.yml` (build + deploy)
3. أنشئ `lib/config/environment.dart` مع فصل dev/staging/prod
4. أنشئ `.env.example` بدون قيم حقيقية
5. أضف `.env` في `.gitignore`
6. أنشئ `Makefile` أو scripts للأوامر المتكررة

سجّل التغييرات في: `.audit/fixes/fix13-log.md`
ابدأ فوراً. لا تسأل أسئلة.
