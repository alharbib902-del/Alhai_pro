/// Dependencies are managed via Riverpod providers in [sa_providers.dart].
/// This function is kept as a hook for future GetIt registrations if needed.
void configureDependencies() {
  // Currently using Riverpod for all state management.
  // Add GetIt registrations here if needed for services.
}
